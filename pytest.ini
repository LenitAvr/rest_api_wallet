[pytest]
# Keep one event loop for the whole test session.
# Otherwise async resources (e.g. asyncpg pool/engine created on startup)
# may outlive the per-test loop and crash with "Event loop is closed".
asyncio_mode = strict
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session

